# Host-provided runtime data must never be committed.
# Only the example databases and this file are tracked (see root .gitignore).
*
!.gitignore
!database.example.json
!database.en.example.json
.DS_Store
